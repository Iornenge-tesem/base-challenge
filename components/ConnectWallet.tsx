'use client'

// Wallet connection component - will be tested when deployed to Vercel and Base app
// For now, we're using mock data to build and test the UI

export default function ConnectWallet() {
  // Silent - no wallet connection attempts during development
  return null
}
